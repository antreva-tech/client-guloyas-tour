# =============================================================================
# Guloyas Tours — Environment Variables
# Copy to .env and fill in values. See docs/client-info.md for client details.
# =============================================================================

# -----------------------------------------------------------------------------
# Brand (client customization)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_BRAND_NAME="Guloyas Tours"
NEXT_PUBLIC_BRAND_TAGLINE="Tours y Experiencias"
# Must match your live domain for link preview image (e.g. https://guloyastours.com)
NEXT_PUBLIC_SITE_URL="https://example.com"
NEXT_PUBLIC_LOGO_PATH="/logo.png"
# Optional: dedicated OG image for link preview (1200×630). If unset, logo is used. Path or absolute URL.
# NEXT_PUBLIC_OG_IMAGE="/og.png"
NEXT_PUBLIC_WHATSAPP_NUMBER="18297188926"
NEXT_PUBLIC_WHATSAPP_MESSAGE="Hello, I'd like more information."
NEXT_PUBLIC_CONTACT_EMAIL="guloyastours@gmail.com"
NEXT_PUBLIC_CONTACT_EMAIL_2=""
NEXT_PUBLIC_ADDRESS_STREET="C/ Casimira Hereaux 4, Enriquillo"
NEXT_PUBLIC_ADDRESS_CITY="San Pedro de Macorís DR 21000"
NEXT_PUBLIC_ADDRESS_COUNTRY="Dominican Republic"
NEXT_PUBLIC_INSTAGRAM_HANDLE=""
NEXT_PUBLIC_INSTAGRAM_URL=""
NEXT_PUBLIC_FOUNDED_YEAR=""
# COMPANY_RNC (optional — for invoice PDF footer, e.g. 132-93225-2)
# COMPANY_RNC="132-93225-2"

# -----------------------------------------------------------------------------
# Database (required)
# Neon: set only DATABASE_URL (pooled URL from dashboard). DIRECT_URL is derived for migrations.
# Local Postgres: set both to the same URL (e.g. localhost).
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
# DIRECT_URL optional for Neon (derived from DATABASE_URL). Required for local Postgres.
# DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# -----------------------------------------------------------------------------
# Session (required — generate: openssl rand -hex 32)
# -----------------------------------------------------------------------------
SESSION_SECRET=""

# -----------------------------------------------------------------------------
# Admin credentials (required — use scripts or hash generator)
# -----------------------------------------------------------------------------
ADMIN_PASSWORD_HASH=""
SUPPORT_PASSWORD_HASH=""

# -----------------------------------------------------------------------------
# Cron (required for Vercel cron jobs)
# -----------------------------------------------------------------------------
CRON_SECRET=""

# -----------------------------------------------------------------------------
# WhatsApp Business Cloud API (optional — for dashboard send / mass message to tour)
# Get from Meta: developers.facebook.com > My Apps > WhatsApp > API Setup
# -----------------------------------------------------------------------------
# WHATSAPP_PHONE_NUMBER_ID=""
# WHATSAPP_ACCESS_TOKEN=""
# WHATSAPP_VERIFY_TOKEN=""

# -----------------------------------------------------------------------------
# Rate limiting (optional — recommended for production)
# -----------------------------------------------------------------------------
# UPSTASH_REDIS_REST_URL=""
# UPSTASH_REDIS_REST_TOKEN=""
